#require 'autotest/growl'

Autotest.add_hook :initialize do |autotest|
  %w{bin coverage doc lib/template log tmp .git .yardoc spec/integration}.each do |exception|
    autotest.add_exception(exception)
  end
end

