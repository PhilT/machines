ec2:
  access_key_id: 123
  secret_access_key: 456

  # 32 bit instances - t1.micro, m1.small, m1.medium
  # 64 bit instances - m1.large, m1.xlarge, c1.xlarge, cc1.4xlarge, cg1.4xlarge, m2.xlarge, m2.2xlarge, m2.4xlarge
  type: m1.large
  ami: ami-379ea943 # EU WEST 1 Ubuntu x64 11.04 EBS - Choose your preferred AMI at http://alestic.com/
  endpoint: ec2.eu-west-1.amazonaws.com
  private_key_file: certificates/amazon.key
  security_group: eu-public

# These are added to /etc/hosts file as 127.0.0.1 hosts for use on development installs
localhosts:
  - wiki.local
  - todo.local
  - example.local
  - admin.example.local

# Extra hosts added to /etc/hosts for build servers, live servers etc
hosts:
  - 192.168.1.4 build
  - 192.168.1.30 prod

timezone: GB
webserver: nginx
db:
  adapter: mysql
  encoding: utf8
  root_pass: Uf6h3HzU4q
  replication_pass: tzIrh2eKjH

awstats:
  stats_path: /var/www/nginx/webstats

# Latest version including preferred nginx version:
# https://github.com/FooBarWidget/passenger/blob/master/NEWS
passenger:
  version: 3.0.9
nginx:
  version: 1.0.6
  url: http://nginx.org/download/nginx-1.0.6.tar.gz
  path: /opt/nginx
  servers_dir: servers

rvm:
  url: https://rvm.beginrescueend.com/install/rvm
  version: 1.8.0

ruby:
  version: 1.9.2

appsroots:
  phil: /home/phil/workspace
  www-data: /var/www/nginx

mail:
  domain: example.com

monit:
  mailto: admin@example.com
  check:
    paths: /
  webserver:
    address: 192.168.1.0/255.255.255.0

login_theme: dark

